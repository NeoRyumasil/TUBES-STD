#include <iostream>

using namespace std;

int main()
{
    listDokter lDokter;
    listPasien lPasien;
    listRelasi lRelasi;

    createListDokter(lDokter);
    createListPasien(lPasien);
    createListRelasi(lRelasi);
}
